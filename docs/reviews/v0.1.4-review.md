# v0.1.4 review / debug

状态：0.1.4 候选审查完成，正式发布仍以远端 CI、标签和 Release 为准。AI-COMPAT-01，追踪 [#27](https://github.com/weihaoting123-ctrl/human-online-relationships/issues/27)，修复 DeepSeek V4.1 Flash 的模型别名和有界 JSON 报告参数兼容。

## 范围与依据

- 2026-09-11 核验 [官方 API 文档](https://api-docs.deepseek.com/)、[V4.1 Flash 公告](https://deepseek.com/news/deepseek-v4-1-flash/) 和 [Chat Completions 参数](https://api-docs.deepseek.com/api/create-chat-completion/)：调用名为 `deepseek-flash`，支持 `thinking.type=disabled`，默认启用思考。
- 采用精确别名兼容加既有 `deepseek-v4-` 前缀。没有猜测 `deepseek-v4.1-pro` 等未核实名称，也没有向其他模型自动附加新参数。别名今后可能由服务商切换底层版本。
- 仅修改后端参数条件、前端新配置默认值及合成测试/版本记录；不改提示词、输出预算、模型目的地址、数据库、归档、备份、定时任务或授权。
- 不读取真实归档、凭据或私人配置；不运行微信或调用真实云模型。公开源码不包含私人部署的配置更新，不自动升级私人运行实例。

## 实施与兼容检查

- TDD：先运行 6 项聚焦用例，观察两处预期失败：新模型缺少 `thinking` 字段、UI 默认仍为旧模型；其余旧行为保护通过。最小补丁后 6 项全部通过。
- 新别名使用固定 DeepSeek 地址、JSON 格式、2,400 输出 token 和非思考模式；既有 V4、旧模型和相似自定义模型保留原行为。
- 留空密钥沿用既有本机加密值，保存阶段不解密也不联网；配置 revision 更新使旧预览失效，后续分析需重新核对范围和确认。
- 已保存旧模型不因加载设置页就迁移；自定义模型不被默认值覆盖。新的默认值不代表服务商账户权限或真实调用成功。
- 独立只读审查本轮 7 个文件，未发现阻塞性缺陷；核对精确条件、非静默迁移、隐私/授权边界及合成覆盖。官方事实以主维护流程已核验资料为前提，静态审查不冒充真实调用验收。

## 验证

- 定向 red：6 项，2 个预期失败；green：6 项通过，4.765 秒。
- 开启 `SHE_LOVE_ME_UI_TESTS=1`、关闭截图，使用项目内合成临时目录运行完整 `python -X utf8 -m unittest discover -s tests -p 'test_*.py' -v`：693 项，499.487 秒，0 失败，9 个既有 Windows 符号链接权限跳过记录（含子例，不按总项数相减冒充通过的方法数）。
- `node --test tests/test_catalog.cjs`：8/8；修改的 JavaScript 语法检查和 `git diff --check` 通过。
- 公开源码 index/已跟踪 worktree 扫描通过；已审閱的 192 个跟踪源码加本轮新 review 文件组成 193 文件独立候选树，`--tree` 扫描通过。没有暂存真实资料或把运行区整体复制到候选；最终发布仍需核对实际提交树与对应 CI。

## 发布与回退

本记录只证明候选源码的本机验证，不是标签、Release 或远端 CI 成功声明；远端发布须核对精确 diff、干净提交树和相应 CI。
源码回退只需还原本轮参数条件和默认值，不删除配置、报告或旧分段缓存。真实配置切回旧模型仍须用户通过原设置流程保存并重新预览；不能恢复已失效的发送确认。
合成回归不等于真实模型计费请求、微信采集、macOS 或人工辅助技术验收。
