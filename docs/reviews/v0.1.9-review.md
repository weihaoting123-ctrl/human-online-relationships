# v0.1.9 candidate review · UI-05

Scope: [#38](https://github.com/weihaoting123-ctrl/human-online-relationships/issues/38),
the existing contact-density calendar only. The public source base is v0.1.8
candidate. This record is AI-assisted code/UI verification, not a human assistive
technology audit or a formal release approval.

## TDD and independent review

- RED: the old mini-date cells contained no date text. The updated existing test
  failed expecting `1`; the old future-date hatch also failed the no-gradient
  expectation. The new six-test suite initially failed 22 subtest assertions for
  missing weekday/date/readout/key/units and cramped layout; keyboard behavior
  already passed. No test used real archives or providers.
- Added nine focused browser tests in total, including cross-month readout reset,
  two independent calendars, rerender, touch, Enter/Space, day arrow navigation,
  zero/outside/future meaning and actual container widths.
- Independent review found no blocking regression: totals callers consistently
  use the structured label; selectors remain scoped to each renderer; no nested
  button/tabstop was added; existing data validation and heat bins are unchanged.
- New tests: **9 passed**, 7.496 seconds. Existing calendar tests: **8 passed**.
  On the deployed static files, the combined suite: **17 passed**, 12.117 seconds.
  Browser request and script-error collections remained empty.

## Visual checks

Inspected four wholly synthetic module captures: year light/dark and month mobile
light/dark. Captures remain in ignored temporary storage, not public artifacts.
Numbers, weekday alignment, nearby hover data, emphasized totals and state keys
are readable. Geometry tests cover 320/390/768/1100-pixel viewports and a narrow
420-pixel container on a desktop viewport. Text is not squeezed into fixed
two-column phone mini-calendars; full-month controls keep comfortable height.

The retained/new rose text pairs were checked against their solid backgrounds:
minimum calculated contrast is 5.27:1 in light mode and 4.70:1 in dark mode.
The renderer retains reduced-motion behavior and explicit keyboard focus. This
does not complete the separate screen-reader/manual accessibility backlog item.

## Regression and delivery gates

- Full frozen synthetic Python/UI suite: **814 run, zero failures, 9 skips**,
  357.860 seconds. Skips are not counted as passes.
- Node catalog tests: **8/8 passed**. JavaScript syntax and whitespace checks pass.
- Initial staged-index plus tracked-working-tree public-source scan passed;
  final commit/archive scan and remote tree identity are checked at publication.
- Private deployment: only the two calendar static source files were changed,
  after matching them to the reviewed base and verifying source rollback copies.
  Both served resources match the reviewed new files and return `no-store`.
  No restart, archive access, cloud call, synchronization, backup run, settings
  change, database migration or history rewrite was performed.
- Candidate publication/CI are tracked in the Issue and PR. The preceding
  candidate's remote CI was failed when checked; this change does not claim to
  repair that unrelated baseline or to make the dependent candidate chain a
  formal Release. Exact current CI evidence is required before merging/releasing.

Rollback is the two previous static files only. Existing reports and source
archives remain intact; refreshing a page does not authorize new analysis.
