# v0.1.10 candidate review · AI-06

Scope: [#40](https://github.com/weihaoting123-ctrl/human-online-relationships/issues/40),
[atomic event-state output plan](../plans/2026-09-15-atomic-event-state.md).
This is AI-assisted review and synthetic verification, not a paid live-model
quality test or a guarantee that a provider will always follow JSON instructions.

## Evidence and root-cause boundary

The observed diagnostic locates the first event's cross-field state validation.
The existing prompt and validator did not contradict each other: the output was
allowed to choose three enums separately, and their combination could conflict.
The old three branches share one diagnostic, and failed response text is not
stored, so no specific original tuple or event meaning has been reconstructed.
No chat text, account, key or failed model body was needed for development.

## Change and safety review

New provider events choose one of 28 fixed exact atomic strings, after complete
eight-field wire-shape validation. Decoding produces the old ten-field canonical
DTO; legacy events keep their validation. No fuzzy parsing, defaulting, status
upgrade/downgrade, input mutation or event dropping is introduced. All evidence,
date, length, links and immutable merge checks follow the decoding step.

Independent old-HEAD differential checks (in-memory only):

- 120 cases: all 60 kind/status/level combinations with and without references.
  Legacy acceptance and error code/field match the old validator. Every accepted
  wire result equals the corresponding canonical result; no old illegal state
  is admitted by wire decoding.
- 784 merge-state transitions (28 × 28): results and rejections agree with the
  old canonical validator, including legal codes that would alter original facts.
- Fixed mapping order, canonical merge input examples and wire output examples
  were reviewed. Removing a redundant terminal-state reference condition does
  not weaken evidence checks: reported/inferred still require references first.
- New diagnostic paths are allowlisted on server and browser; provider values,
  malicious paths and arbitrary detail prose are not rendered or retained.

No blocking finding remained in independent backend review.

## Test evidence

- RED: valid atomic event rejected as missing legacy fields; safe error path
  rejected. New UI diagnostics lost segment/call context in three cases before
  the browser field allowlist was updated.
- Focused backend tests: 68 passed (1.322 seconds), including the complete state
  matrix, canonical equality, immutable merge and existing contracts/focus rules.
- Real response parser with mocked urllib: four new pipeline tests passed.
  Three synthetic leaves plus two hierarchical merges save canonical results;
  a second execution uses five cached calls without transport. Invalid enum,
  type and mixed shape stop on attempt one with no result seal or report and no
  automatic retry. This is not a real provider request.
- New plus existing diagnostic browser tests: 11 passed. The pipeline/browser
  combined run took 18.151 seconds; authorization remained unchecked on failure.
- Node catalog tests: 8/8; JavaScript syntax and whitespace checks passed.
- Full frozen Python/UI regression: 831 tests in 371.858 seconds, no failures,
  nine expected platform-dependent skips.

## Deployment and publication

Only four runtime source files were deployed after verifying base parity and
source-only rollback copies. Analysis, sync and backup were idle before the
scoped service restart. No data, reports, credentials, connection settings or
caches were migrated or deleted. Existing job states remained unchanged.

- Private runtime imports passed; all four deployed files match reviewed source.
- Private runtime synthetic regression: 55 tests in 19.242 seconds, all passed,
  including backend contracts, mocked transport and browser error handling.
- Restarted loopback service: root page and JavaScript both returned HTTP 200;
  served JavaScript matches deployed source. No real analysis was submitted.

The actual prompt changes preview/cache identity; old previews need fresh
confirmation and old caches may not be reusable. Old failed tasks remain failed;
deployment never resubmits them. Source scans, exact remote tree identity and
current CI are publication gates; candidate/dependent branches are not a formal
Release until those gates and their baselines are satisfied.
