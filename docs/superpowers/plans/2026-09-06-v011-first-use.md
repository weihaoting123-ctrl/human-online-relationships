# v0.1.1 First-use Flow Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 UX-01 至 UX-05，并按用户要求自行完成全项目关键边界 review、合成回归和 debug 后发布 v0.1.1。

**Architecture:** 保持现有 HTTP/文件/SQLite 和前端工作区。只修正客户端文件、请求、导航、焦点及异步状态；独立审查和发布由主代理负责，不重构数据路径。

**Tech Stack:** Python 3.12、unittest、Playwright、原生 JavaScript、现有 CSS token、Node 目录测试。

---

规范：[首次使用设计](../specs/2026-09-06-v011-first-use-design.md)。公开源码开发分支：`codex/v0.1.1-import-flow`；不使用私人部署或默认 8765 端口。临时材料仅放 `scripts/tmp`。同一前端流程由一个实现者拥有，其他代理只读审查，避免共享文件冲突。

## Task 1：测试驱动修复首次使用流程

**Files:**

- Modify: `dashboard/static/app.js`、`core.js`、`shell.js`、`index.html`。
- Modify only if needed: `dashboard/static/apple-ui.css`。
- Create: `tests/test_import_navigation_ui.py`。
- Test: `tests/test_dashboard_ui.py`、`test_library_ui.py`、`test_analysis_ui.py`、`test_apple_ui.py`。

- [x] 使用 `test_library_ui.LibraryBrowserTests` 的静态夹具，不继承其测试方法；包装其 setUp/tearDown，保留本机请求与无异常断言。

```python
import os
import unittest
import test_library_ui as fixtures

@unittest.skipUnless(os.environ.get('SHE_LOVE_ME_UI_TESTS') == '1', 'Opt-in synthetic UI')
class ImportNavigationBrowserTests(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        fixtures.LibraryBrowserTests.setUpClass()

    @classmethod
    def tearDownClass(cls):
        fixtures.LibraryBrowserTests.tearDownClass()

    def setUp(self):
        self.fixture = fixtures.LibraryBrowserTests()
        self.fixture.setUp()
        self.addCleanup(self.fixture.doCleanups)
        self.page, self.expect = self.fixture.page, self.fixture.expect

    def tearDown(self):
        self.fixture.tearDown()

    def test_new_json_file_does_not_keep_markdown_source(self):
        self.page.locator('a[data-view="maintenance"]').click()
        self.page.locator('#chat-file').set_input_files(
            {'name': 'synthetic.md', 'mimeType': 'text/plain', 'buffer': b'fiction'})
        self.expect(self.page.locator('#source-kind')).to_have_value('markdown')
        self.page.locator('#chat-file').set_input_files(
            {'name': 'synthetic.json', 'mimeType': 'application/json', 'buffer': b'{}'})
        self.expect(self.page.locator('#source-kind')).to_have_value('auto')
```

- [x] 运行新测试观察旧代码失败；失败应是来源仍为 markdown，而不是夹具导入或控件超时。每项修复均先建立对应失败用例。

```powershell
$env:TEMP = (Resolve-Path scripts/tmp).Path
$env:TMP = $env:TEMP
$env:SHE_LOVE_ME_UI_TESTS = '1'
$env:SHE_LOVE_ME_UI_SCREENSHOTS = '0'
.\.venv\Scripts\python.exe -X utf8 -m unittest discover -s tests -p test_import_navigation_ui.py -v
```

- [x] 用共同选文件函数替换 change/drop 两处状态赋值；该函数只在选择新文件时调用，提交不覆盖用户当前手动来源。

```javascript
function selectImportFile(file) {
  state.selectedFile = file || null;
  $('#file-label').textContent = file ? file.name : '选择聊天导出文件';
  $('#source-kind').value = file && /\.(md|txt)$/i.test(file.name) ? 'markdown' : 'auto';
}
```

- [x] 增加以下真实浏览器行为断言后逐项修复：拖放双向切换；当前文件手动来源保留；重复点击只提交一次；成功展示正确会话；确认成功但状态刷新失败仍显示成功；等待中导航/更换文件不会抢焦点或清新输入。拦截 `/api/import` 返回夹具已有会话详情，并检查真实渲染，禁止把 `renderBundle` mock 掉。
- [x] 维护状态快照：提交时捕获文件对象、字段值、导航代次、选择代次；提交完成时先保存结果，随后刷新；只有快照仍匹配才清理输入和自动跳转。迟到成功以页面内结果按钮供用户显式查看，不调用模型。
- [x] 增加零会话与筛选无匹配/读取失败用例。成功读取后设置目录加载状态；`catalog-empty` 仅在 `loaded && bundles.length > 0 && resultCount === 0` 时显示，首次引导仅 `loaded && bundles.length === 0` 时显示。
- [x] 增加返回/前进与焦点用例，并断言 URL 不含虚构查询词；修改 shell 的主动导航为 pushState、初始化/无效路由为 replaceState、历史恢复为不写历史。保持 `navigate(view, updateHash)` 兼容，不能破坏其他工作区的程序调用。

```javascript
const target = `#${selected}`;
if (updateHash && location.hash !== target) history.pushState(null, '', target);
// history 恢复通过 navigate(view, false)，不得再次 pushState。
```

- [x] 打开详情后聚焦带 `tabindex="-1"` 的标题。关闭时按会话身份重新定位刚重绘的按钮；目标不可用则聚焦搜索框。后台刷新不执行该聚焦步骤。减少动态效果的滚动选择：

```javascript
function preferredScrollBehavior() {
  return window.matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth';
}
```

- [x] 给共享 API 增加网络/无效响应/空对象/取消/业务拒绝用例：只 mock fetch 响应，不 mock `api()`；保留 409 与 AbortError，技术响应原文不进入提示，所有写入结果未知场景检查请求计数始终为 1。错误结果保持 `message/status/name` 兼容，可加固定 `code/outcomeUnknown` 字段，不增重试或全局超时。
- [x] 运行新套件、现有目录/管理/分析/主题套件；检查无可选 AI 脚本情况下导航仍可用。只有正确 GREEN 后做小规模去重重构。
- [x] 实现者自查 diff，返回各条 RED/GREEN、修改文件与遗留疑点；不自行操作 GitHub 或升级版本。主代理独立核对规范，再交由独立代码审查者评审。

## Task 2：完整 review 与 debug

**Files:**

- Review: 全部公开 `dashboard/`、关键 `scripts/` 与所有合成测试。
- Create: `docs/reviews/v0.1.1-review.md`（只记录适合公开的结论与验证范围）。
- Fix: 限于本轮已授权修复及必要回归；独立较大/安全设计保持私下审查，不公开未修复漏洞细节。

- [x] 独立规范审查逐项对应 UX-01 至 UX-05，核对非目标未扩大。
- [x] 规范通过后独立质量审查异步代次、焦点/历史、输入快照、错误语义、API 调用数、数据/授权边界和测试是否确实验证业务。
- [x] 对审查发现写合成失败回归，最小修复，再重跑相关测试；不能只增加等待时间或放宽断言隐藏缺陷。
- [x] 用以下完整命令验证，并保留退出码、通过/跳过/失败数量；浏览器截图关闭，不上传运行材料。

```powershell
$env:TEMP = (Resolve-Path scripts/tmp).Path
$env:TMP = $env:TEMP
$env:SHE_LOVE_ME_UI_TESTS = '1'
$env:SHE_LOVE_ME_UI_SCREENSHOTS = '0'
.\.venv\Scripts\python.exe -X utf8 -m unittest discover -s tests -p 'test_*.py'
node --test tests/test_catalog.cjs
Get-ChildItem dashboard/static -Filter '*.js' | ForEach-Object { node --check $_.FullName; if ($LASTEXITCODE -ne 0) { throw 'JavaScript syntax failed' } }
git diff --check
```

- [x] 审查记录区分本轮修复、完整测试覆盖、现存技术债、未验证实机/人工读屏，不宣称渗透测试或全平台认证。

## Task 3：独立版本、GitHub 与发布核验

**Files:** `VERSION`、`CHANGELOG.md`、`BACKLOG.md`、`ROADMAP.md`、`docs/release-verification.md`。

以下为提交前检查点快照；远端动作完成后在同版 Release 记录提交、CI 与核验结果，不通过预先勾选宣称已发布，也不为更新勾选改写已发布标签。

- [x] 将 VERSION 更新为单行 `0.1.1`，CHANGELOG 新增实际完成项和限制；UX-01 至 UX-05 只在验收后标为完成，其他候选保持原状态。
- [x] 只暂存审阅过的精确文件集合，运行公开扫描；不使用 `git add .`。

```powershell
.\.venv\Scripts\python.exe -X utf8 scripts/check_public_release.py --root .
if ($LASTEXITCODE -ne 0) { throw 'Publication guard failed' }
```

- [ ] 创建有界 PR，使用匿名提交身份，远端 tree 与本机审阅 index 一致；读取对应 CI 结果并处理失败，不拿旧基线通过替代。
- [ ] 合并前确认 Critical/Important 审查项已解决；在已验证提交创建不可变 `v0.1.1` 标签和 Release，不重写 v0.1.0。
- [ ] 从已审阅提交生成独立源码树，用 `--tree` 再次扫描；核对 VERSION、CHANGELOG、tag、Release 指向同一版本。关闭完成的 Issue #12/#13 并链接证据，保留其余待办。
- [ ] 本机工作区与远端已验证提交对齐，确认无意外未提交改动；最终说明发布结果、测试数量、已知限制与私人部署是否更新。
