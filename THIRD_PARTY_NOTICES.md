# 来源与第三方说明

## 上游代码

本仓库由 MIT 许可的 [863401402/she-love-me](https://github.com/863401402/she-love-me) 派生，初始上游基线为 `80aec1d`。根目录 `LICENSE` 保留原始版权及完整许可文字，不将上游作品署名替换为本项目维护者。

后续增加了本地控制台、只读增量归档、媒体与语音、独立备份、模块管理、SQLite 人工管理层、按次授权 AI 以及关系时间线。公开 `v0.1.0` 指本派生项目的首次公开基线，不冒称上游全部代码由本项目原创。

为保护私人运行资料，公开仓库采用重新核查的源码快照和独立提交历史。没有复制原 Git 对象、运行数据、上游宣传二维码或来源不明的截图。

## 按需安装的外部组件

- Python 依赖：见 `requirements.txt` 与 `requirements-dev.txt`，分别适用各包自带许可。
- 微信读取器：项目内引导器固定版本并校验来源，不将下载的包、原生二进制或缓存提交到本仓库。
- 语音转写：按需下载官方 sherpa-onnx / SenseVoice 等依赖与模型；模型权重、音频和识别结果不随源码发布。
- OpenAI / DeepSeek：可选外部 API，不附带共享凭据。使用者自己配置账户并承担调用费用及数据处理政策。

修改下载来源、版本、模型或分发方式前，请重新核查对应组件的许可及完整性；本说明不替代各组件的完整许可。
