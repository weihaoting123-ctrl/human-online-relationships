# 安全与隐私政策

「人类线上关系可视化」处理的资料可能包含高度私密的第三方聊天。本项目公开仓库只接收源代码和合成复现，不接收个人聊天资料。

## 私下报告漏洞

发现认证绕过、未授权上传、凭据泄漏、路径越界、归档被覆盖或其他潜在漏洞时，**不要开公开 Issue、PR 或讨论串披露细节**。

请使用 GitHub 的 [私下漏洞报告入口](https://github.com/weihaoting123-ctrl/human-online-relationships/security/advisories/new)。该入口需要仓库启用 Private vulnerability reporting；若入口尚不可用，请勿改为公开粘贴漏洞详情或敏感附件，等待私下通道可用。

报告只需包含：

- 受影响的公开版本或提交，以及操作系统/浏览器版本，不含本机用户名、账号或完整路径。
- 使用虚构数据的最小复现、预期边界与实际行为。
- 可能影响的模块，以及不依赖真实聊天的影响说明。
- 可选修复思路；不要发送有效凭据、数据库、聊天/音频、完整日志或真实截图，即使在私下报告中也一样。

维护者会根据影响核验并协调修复与披露。目前没有响应时间 SLA、漏洞赏金、正式认证或长期支持承诺。维护目标是当前公开基线 `0.1.x` 的最新补丁；未来支持范围随版本更新。未发布代码和旧版问题仍欢迎私下告知，但不承诺回移所有修复。

## 使用与开发边界

- 只处理你有权处理的资料，并尊重聊天中其他人的隐私与同意。不要将私人数据交给公开 Issue、CI、在线代码审查或第三方调试站点。
- 默认本机处理不等于所有功能离线：依赖安装、明确选择的导出器资源获取和按次授权的云分析可能联网。发送前核对实际接收方与范围。
- 自动脱敏是尽力减少暴露，不能保证自由文本匿名。AI 推断不是心理诊断、关系事实或安全结论。
- 控制台面向回环地址上的个人本机使用，不应绑定公网、反向代理成共享服务或绕过会话/来源检查。
- 备份同样私密；隔离恢复不等于备份可公开共享。不要把备份、截图或日志误当作无敏感信息的调试材料。
- 测试只使用完全合成的样本和 mock；不通过真实账户、个人聊天或真实付费调用验证贡献。

## 如果意外公开了私人资料

立即停止继续传播；撤销或轮换可能泄漏的凭据，通过平台的私下支持/移除流程处理已公开内容，并核对暴露范围。删除 Issue、附件或 Git 提交不能保证副本、通知和缓存消失。联系维护者时只提供定位所需的最少信息，不再次复制敏感正文。

## 审查说明

仓库文档、代码整理和安全检查可能使用 AI 辅助。现有检查以代码边界与合成测试为主，**不是渗透测试、安全认证或“绝对安全”的保证**。没有发现问题不等于没有漏洞。贡献者仍须人工核对提交内容、权限边界和第三方依赖来源。
