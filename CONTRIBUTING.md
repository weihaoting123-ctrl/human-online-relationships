# 参与贡献

欢迎为「人类线上关系可视化」改进代码、测试和文档。公开仓库是源代码与合成示例的协作空间，不是聊天资料托管站。

## 先确认边界

- Issue、PR、附件、截图、日志和测试夹具中不得包含真实聊天、联系人、账号、手机号、邮箱、本机用户名或路径、数据库、音频、密钥、Token、Cookie。仅使用完全虚构的合成复现；给真实资料打码不等于合成资料。
- 不提交 `data/`、`reports/`、`backups/`、本机运行缓存或凭据。`.gitignore` 不是保密保证，提交前仍须检查暂存内容。
- 安全漏洞和可能的数据泄露不要开公开 Issue；按 [安全政策](SECURITY.md) 私下报告。即使私下报告，也不要附真实聊天或有效密钥。
- 日常测试不连接微信、不读取真实归档、不运行真实同步/备份、不调用付费模型。传输与外部服务必须使用合成响应或 mock。
- 原始聊天只读。标签、别名、备注、置顶和回收站属于独立管理层；隐藏不等于物理删除。

公共产品名为「人类线上关系可视化」，仓库名为 `human-online-relationships`。`she-love-me` 仍用于内部技能 ID、部分兼容入口和历史格式；不要顺手重命名这些标识、数据包 ID 或已有数据库。

## 从一个小问题开始

1. 搜索已有 Issue，普通缺陷或建议使用对应表单。
2. 说明目标、模块、当前行为和期望行为；复杂变更先讨论接口和隐私影响。
3. 一个 PR 解决一个有界问题。功能和修复先补失败的合成测试，再实现并记录通过结果。
4. 更新涉及的文档与 [CHANGELOG](CHANGELOG.md)，提交可复现的验收证据。只改文档时可做链接、结构与示例检查，无需声称跑过无关测试。

不要求贡献者为复现配置个人微信账号或云端密钥。不能用合成材料复现时，先说明无法复现的条件，不索取对方的真实聊天。

## 模块与标签

维护者为每个 Issue 选择至少一个 `area:*`、一个 `type:*` 和一个 `priority:*`。表单默认只添加类型和待分诊标签；选择模块不会自动添加对应标签，需要维护者核对。

| 模块标签 | 范围 |
| --- | --- |
| `area:dashboard` | 页面壳、导航、表单、主题与无障碍 |
| `area:timeline` | 本机时间统计、事件显示、覆盖与关联 |
| `area:ai` | 逐次授权、模型响应约束、分段、缓存与费用保护 |
| `area:library` | 管理数据库、别名、备注、标签和回收站 |
| `area:import-sync` | 格式导入、只读同步与本地流水线 |
| `area:media-voice` | 图片、语音、本地转写与派生索引 |
| `area:backup` | 不可变快照、校验与隔离恢复 |
| `area:privacy-security` | 隐私与安全改进；未修复漏洞仍只私下讨论 |
| `area:docs` | 安装、使用、开发和维护文档 |
| `area:ci` | 合成测试、持续集成与发布检查 |

类型标签：`type:bug`（缺陷）、`type:feature`（新增能力）、`type:docs`（文档）、`type:maintenance`（维护）、`type:test`（测试）。

| 优先级 | 分诊标准 |
| --- | --- |
| `priority:P0` | 已确认的严重安全/隐私风险或不可恢复数据损失；先私下处置，不公开漏洞细节 |
| `priority:P1` | 核心流程不可用、授权/数据边界错误，或没有合理绕行方式的严重回归 |
| `priority:P2` | 常规缺陷和已接受的迭代功能；默认优先级 |
| `priority:P3` | 可延后的体验、文案和低影响改进 |

`status:needs-triage` 表示尚未核对范围和优先级。优先级不是修复时限或服务等级承诺。里程碑表达计划版本，Issue 中的验收条件才决定是否完成。

可复用的标签、里程碑和 Issue 种子见 [待应用管理配置](.github/project-management.json)。它是供维护者核对的静态配置，状态为 `pending`，不会自动建立 GitHub 资源；应用前按名称检查已有资源以免重复。功能验收仍以 [ROADMAP](ROADMAP.md) 为准，配置存在不表示发布或迭代已经完成。

## 本机开发与验证

先按 [README](README.md) 准备 Python、Node 和所需测试依赖。维护架构见 `.agents/skills/she-love-me/references/local-architecture.md`；以当前克隆目录为工作目录，不使用维护者机器的绝对路径。

Windows PowerShell 的合成回归入口：

```powershell
$taskTemp = Join-Path (Get-Location) 'scripts\tmp'
New-Item -ItemType Directory -Force -Path $taskTemp | Out-Null
$env:TEMP = $taskTemp
$env:TMP = $taskTemp
$env:SHE_LOVE_ME_UI_TESTS = '1'
.\.venv\Scripts\python.exe -X utf8 -m unittest discover -s tests -p 'test_*.py'
node --test tests\test_catalog.cjs
```

UI 测试需要已安装的测试浏览器；缺少依赖或平台不支持导致的跳过必须写明，不能算作通过。浏览器夹具使用合成数据和随机回环端口，不打开真实控制台做截图。允许先运行相关模块测试；合并前由维护者核对需要的完整回归。

变更检查重点：

- AI：不得新增静默上传、自动重试、后台付费调用或替旧报告自动补跑；改变提示词后，旧预览必须重新确认。
- 时间线：记录空白不等于现实断联，计划日期已过不等于实现；统计与模型推断分开，部分覆盖明确显示。
- 管理库：保持不可变身份、乐观版本冲突和软删除语义；迁移失败不能通过删除重建库处理。
- 备份：保留旧清单版本；恢复到新目录，不覆盖活动数据。
- UI：检查键盘、焦点、浅/深色、小屏与长内容；授权选项不得默认勾选。

## PR 与发布

PR 写明关联 Issue、模块、变更范围、验证命令及结果、兼容性和回退方式。涉及数据库、权限、传输或费用的变更，应明确列出影响；不要把 AI 生成的评审结论当作人工验收或渗透测试。

版本号记录在 [VERSION](VERSION)，发布说明记录在 [CHANGELOG](CHANGELOG.md)。`v0.1.0` 是本仓库首个公开基线，不继承上游的版本编号或将上游历史记作本仓库原创。保留上游许可证、来源说明和第三方声明。未来计划见 [ROADMAP](ROADMAP.md)，未满足验收前不得标记为已交付。
