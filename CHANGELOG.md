# 变更记录

本文件记录「人类线上关系可视化」公开仓库的版本，不复写上游项目历史。内部技能 ID `she-love-me` 和部分历史格式保留兼容。

## 未发布

下一版本候选见 [ROADMAP](ROADMAP.md)。候选不代表已经实现，也不是发布日期承诺。

## 0.1.2 — 2026-09-07 · 离线导入保存安全

DATA-01 第一阶段，追踪 [#22](https://github.com/weihaoting123-ctrl/human-online-relationships/issues/22)；发布状态以 [Release](https://github.com/weihaoting123-ctrl/human-online-relationships/releases/tag/v0.1.2) 为准，证据见 [审查记录](docs/reviews/v0.1.2-review.md)。

- 四个离线转换入口统一完整暂存、不可覆盖的新包发布和核验复用；保留原 CLI 参数和成功字段，增加 `created/reused` 标记。
- 同材料复用前核对明确来源/联系人/账号/转换选项、完整文件集合、摘要和实际内容；新材料另存，旧包及已有分析、报告保持不变。
- 消息与 WeFlow 表情清单一起封存，保留真实重复记录及原有有效/拒绝统计；错误只返回固定说明，不回显输入、路径或 traceback。
- 网页采用 v2 身份清单和消息完整性核验，与离线入口共用进程锁；暂存移出联系人发现范围，保留网页线程串行和原始文件失败回滚。
- 覆盖 Windows 目录联接、隐藏属性、大小写文件别名，以及中断、并发、损坏材料和发布后响应失败的合成回归；补测试子进程就绪超时。
- 记录受托维护的范围选择、评审、发布和隐私保护规则；需求池修订 R3，父需求 #15 保持开放。

兼容取舍：旧网页 v1 清单不自动猜测升级，首次重新导入可能产生新包；网页与 CLI 不保证跨入口复用。旧包不迁移、不删除。共同锁只约束接入的协作入口，不是防任意外部程序或断电的数据保护保证。

本轮不改原生同步检查点、显式合并、旧微信/QQ 在线提取器、显式文件输出、备份和定时任务；不包含导入预检向导或 UI 工作区重组，不自动升级私人部署。详见 [导入保存契约](docs/import-storage.md)。

## 0.1.1 — 2026-09-07 · 首次使用闭环

实际发布状态以对应 [Release](https://github.com/weihaoting123-ctrl/human-online-relationships/releases/tag/v0.1.1) 为准；审查和验证范围见 [review 记录](docs/reviews/v0.1.1-review.md)。

- 首次导入：选择/拖放新文件重新判断来源，保留当前文件的手动选择；异步提交固定文件与字段快照，防止重复提交。
- 导入结果：区分“导入已成功”与“目录刷新失败”；结果可显式打开，迟到响应不清除新输入或覆盖新工作区；重复导入不自动恢复隐藏档案。
- 目录状态：区分首次加载、读取失败、真实空档案与筛选无匹配；刷新保留最近成功目录，元数据写入失效旧请求后不会停在永久加载。
- 导航与键盘：视图前进/后退、首次导入引导、详情标题与按身份返回焦点；URL 只保存视图 ID，尊重减少动态效果设置。
- 请求错误：稳定提示覆盖断网、取消、超时、空/非 JSON 响应及业务拒绝；不展示技术异常原文，保留 HTTP 409 和取消语义，不增加自动重试。
- AI 授权：提交时捕获授权后清空两项确认，包括不确定调用的重试勾选；响应丢失后先读取本机作业/历史，不自动再次调用。
- 需求管理：建立模块解构、分级需求池和稳定 ID；只交付 UX-01 至 UX-05，工作台重组、来源/平台适配和后续作业体系仍是候选。
- 合成测试维护：覆盖导入真实端点、AI/备份已接受响应丢失、交叉异步状态；修复刷新后旧 DOM 测量竞态，保留 44px 门槛；统一截图开关。
- 包含 v0.1.0 之后的 CI 夹具维护：分离导航/控件超时，验证模块拒绝停用后的提示及回滚结果；不重写已有标签。

本轮不改变原始聊天、持久格式、采集/备份/定时任务或模型配置。只更新公开源码，不自动升级私人运行部署；合成回归不等于真实微信采集、计费模型、macOS 或人工读屏验收。

## 0.1.0 — 2026-09-06 · 首个公开基线

此条记录公开源码基线；发布状态以仓库 Release 与标签为准。跨平台合成测试结果和限制见 [公开验证记录](docs/release-verification.md)。

### 当前已实现

- 本地优先的单会话档案浏览、检索与筛选；兼容已有导入和只读归档入口。
- 独立 SQLite 管理层：别名、备注、标签、置顶、隐藏/恢复、版本冲突保护；不通过管理操作覆盖聊天原件。
- 可选模块开关与依赖检查；本地同步流水线聚合失败，不包含自动云端分析步骤。
- 本机关系时间线：记录首末日期、消息频率、较长记录间隔和最近归档消息；显示统计时点与归档限制。
- 按次确认的可选 AI 分析：确认会话、日期范围、接收方和调用计划；有上限分段、成功缓存复用、未知调用重试保护。
- 可选 AI 事件时间线：计划、推进、结果与上下文，带日期和匿名样本位置、依据等级及关联。未联系原因区分自述、推测和未知；部分结果标明覆盖范围。
- 旧报告无时间线时显示未生成，不改写历史文件或自动重跑；提示词变化使旧预览重新确认。
- 媒体/语音档案和本地转写相关入口；语音文字加入 AI 范围仍需显式选择，不上传原始音频。
- 版本化不可变本地备份，兼容旧快照清单，恢复只写新目录。
- 共享浅/深主题、小屏布局、键盘焦点、节点筛选/排序/分页和详情弹窗；合成 Python/浏览器/JavaScript 回归夹具。
- 公开贡献、安全报告、Issue/PR 表单、版本和路线图约定。
- 仅手动触发的 GitHub 管理流程：先预览，按需应用标签与种子里程碑/待办；跳过已有项，保留人工修改，拒绝歧义、删除和自动写入重试。
- CI 使用规范化的项目内合成临时目录；UI 样式断言在同一次浏览器执行中选取当前节点并读取样式，避免刷新时读取已分离控件。

### 边界与未包含能力

- 不跨会话合并人物身份，不提供全联系人社交网络；群聊“其他参与者”不代表单个人。
- 不根据消息量诊断人格、感情或真实动机；没有消息不能证明现实没有联系。
- 时间线是有上限的关键事件整理，不是每条消息的完整语义重建。AI 输出不是已核实事实，抽样及多级摘要会遗漏上下文。
- 不提供受验证的无人值守云分析、完整跨平台微信采集承诺或安全认证。
- 本次审查和文档整理包含 AI 辅助；合成测试与代码审查不等同于渗透测试。

### 来源与版本说明

本项目沿用 `she-love-me` 的部分代码、技能与兼容格式，在其基础上整理本地管理和关系时间线体验。`0.1.0` 是本公开仓库的新基线编号；上游的 v3 等版本、历史成果和作者归属不计作本仓库原创发布。许可证和来源声明以仓库保留的相应文件为准。
